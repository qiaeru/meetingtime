{
  "name": "@meetingtime/shared",
  "version": "1.5.0",
  "private": true,
  "description": "Wire-level Socket.IO contracts and TypeScript models shared between the Meetingtime client and server.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./events": "./dist/events.js",
    "./models": "./dist/models.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "node -e \"for (const p of ['dist', 'tsconfig.tsbuildinfo']) require('node:fs').rmSync(p, { recursive: true, force: true })\""
  }
}
